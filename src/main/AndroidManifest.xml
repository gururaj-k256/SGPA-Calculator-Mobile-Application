<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gpacalculator">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GPACalculator">
        <activity android:name=".EighthSemise" />
        <activity android:name=".SeventhSemise" />
        <activity android:name=".SixthSemise" />
        <activity android:name=".FifthSemise" />
        <activity android:name=".FourthSemise" />
        <activity android:name=".ThirdSemise" />
        <activity android:name=".SecondSem" />
        <activity android:name=".FirstSem" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>